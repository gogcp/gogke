name: terraform-submodules/gke-service-account
on:
  pull_request:
    paths:
      - ".github/workflows/terraform-submodules-gke-service-account.gotmpl.yaml"
      - "terraform-submodules/gke-service-account/**"
      - "!terraform-submodules/gke-service-account/README.md"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/terraform-submodules-gke-service-account.gotmpl.yaml"
      - "terraform-submodules/gke-service-account/**"
      - "!terraform-submodules/gke-service-account/README.md"
jobs:
  terraform-submodules-gke-service-account:
    name: terraform-submodules/gke-service-account
    runs-on: ubuntu-24.04
    container: ghcr.io/damlys/gogcp-devcontainer:0.0.0
    permissions:
      contents: write
      id-token: write
    env:
      TF_IN_AUTOMATION: 1
    steps:
      - name: git clone
        uses: actions/checkout@v4
      - name: gcloud auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}
          create_credentials_file: true
          cleanup_credentials: true
      - name: cache
        uses: actions/cache@v4
        with:
          key: terraform-submodules-gke-service-account
          path: |
            terraform-submodules/gke-service-account/.terraform
            terraform-submodules/gke-service-account/.terraform.lock.hcl
      - name: build
        run: ./scripts/terraform-submodule build "gke-service-account"
      - name: test
        run: ./scripts/terraform-submodule test "gke-service-account"
      - if: github.event_name == 'pull_request' && github.ref == 'refs/heads/main'
        name: pre-publish
        run: ./scripts/terraform-submodule pre-publish "gke-service-account"
      - if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        name: publish
        run: ./scripts/terraform-submodule publish "gke-service-account"
      - name: show
        run: ./scripts/terraform-submodule show "gke-service-account"
