name: terraform-submodules/gcp-terraform-state-bucket
on:
  pull_request:
    paths:
      - ".github/workflows/terraform-submodules-gcp-terraform-state-bucket.gotmpl.yaml"
      - "terraform-submodules/gcp-terraform-state-bucket/**"
      - "!terraform-submodules/gcp-terraform-state-bucket/README.md"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/terraform-submodules-gcp-terraform-state-bucket.gotmpl.yaml"
      - "terraform-submodules/gcp-terraform-state-bucket/**"
      - "!terraform-submodules/gcp-terraform-state-bucket/README.md"
jobs:
  terraform-submodules-gcp-terraform-state-bucket:
    name: terraform-submodules/gcp-terraform-state-bucket
    runs-on: ubuntu-24.04
    container: ghcr.io/damlys/gogcp-devcontainer:0.0.0
    permissions:
      contents: write
      id-token: write
    env:
      TF_IN_AUTOMATION: 1
    steps:
      - name: git clone
        uses: actions/checkout@v4
      - name: gcloud auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}
          create_credentials_file: true
          cleanup_credentials: true
      - name: cache
        uses: actions/cache@v4
        with:
          key: terraform-submodules-gcp-terraform-state-bucket
          path: |
            terraform-submodules/gcp-terraform-state-bucket/.terraform
            terraform-submodules/gcp-terraform-state-bucket/.terraform.lock.hcl
      - name: build
        run: ./scripts/terraform-submodule build "gcp-terraform-state-bucket"
      - name: test
        run: ./scripts/terraform-submodule test "gcp-terraform-state-bucket"
      - if: github.event_name == 'pull_request' && github.ref == 'refs/heads/main'
        name: pre-publish
        run: ./scripts/terraform-submodule pre-publish "gcp-terraform-state-bucket"
      - if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        name: publish
        run: ./scripts/terraform-submodule publish "gcp-terraform-state-bucket"
      - name: show
        run: ./scripts/terraform-submodule show "gcp-terraform-state-bucket"
